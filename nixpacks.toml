[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = [
  "cd client && npm install && npm run build",
  "cd server && npm install && node src/db/migrate.js"
]

[start]
cmd = "node server/src/index.js"
